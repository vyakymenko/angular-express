version: '3'

services:

  web:
    build:
      context: .
      dockerfile: ./.docker/Dockerfile_dev
    command: npm start
    container_name: angular-express
    image: angular-express/dev
    volumes:
      - ./ui:/home/node/angular-express/ui
      - ./server:/home/node/angular-express/server
    networks:
      - dev-network
    ports:
      - '80:4200'
      - '3000:3000'

networks:
  dev-network:
    driver: bridge
